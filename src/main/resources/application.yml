
server:
  port: 8181

# ?? ??
logging:
  level:
    root: info
    com:
      example:
        apibasic:
          req: trace
    org:
      hibernate: info

# swagger-ui custom path
springdoc:
  swagger-ui:
    path: /myapi.html

spring:
  # database connection
  datasource:
    url: jdbc:mariadb://localhost:3306/demodb
    username: root
    password: mariadb
  # jpa config
  jpa:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDB106Dialect # ?? ??
    show-sql: true # ???? SQL? ??? ? ???
    hibernate:
      database: mysql # ????? dbms ??
      generate-ddl: true # ? DBMS? ?? ?? ??? ??? ?? ??
      ddl-auto: update # DDL?? ??? ? ?? create > ??? ?? ? ?? update > ??? ?? ??

# create : ? ???? DDL? ?? ???
# update : ? ???? ????? DDL? ALTER??? ??
# none : DDL? ???? ??  -  ??????? none?? ??.